# Only read by `npm run dev`. Points the host at the backend's own dev port
# so it can fetch /api/extensions and open the SSE stream cross-origin.
# Unset in production builds — see src/config.js.
VITE_API_BASE_URL=http://localhost:5080
